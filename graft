#!/usr/bin/env python3

import random
import sys
from graftlib.main import main, World
from graftlib.realfs import RealFs


exit(
    main(
        World(
            sys.argv,
            sys.stdin,
            sys.stdout,
            sys.stderr,
            random,
            RealFs()
        )
    )
)
